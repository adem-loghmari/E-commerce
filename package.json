{
  "name": "e-commerce",
  "version": "1.0.0",
  "description": "E-commerce application with admin panel",
  "scripts": {
  "start": "cd backend && npm start",
  "build": "npm run install-all && npm run build:frontend && npm run build:admin",
  "build:frontend": "cd frontend && npm run build",
  "build:admin": "cd admin && npm run build",
  "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
  "dev": "concurrently \"npm run dev:backend\" \"npm run start:admin\" \"npm run start:frontend\"",
  "start:backend": "cd backend && npm run dev",
  "start:frontend": "cd frontend && npm start",
  "start:admin": "cd admin && npm start"
},
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}